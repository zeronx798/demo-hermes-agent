_config_version: 33
# 默认模型：MiniMax Coding Plan（国内端点，Anthropic 兼容）
# - key 从环境变量 MINIMAX_API_KEY 读取（不写入此文件，随源码走的只有结构）
# - base_url 走国内端点 api.minimaxi.com（阿里云直连，不经代理）
model:
  provider: custom
  default: deepseek-v4-flash
  base_url: https://api.deepseek.com

platforms:
  api_server:
    port: 8642
    enabled: true
mcp_servers: {}
